{% extends "layouts/default.njk" %}

{% block body %}
  <a href="/app/documents/create" class="btn btn-primary">Criar</a>
  <a href="/signout" class="btn btn-primary">Deslogar</a>

  <table class="table">
    <thead>
      <tr>
        <th scope="col">Código</th>
        <th scope="col">Titulo</th>
        <th scope="col">Departamento</th>
        <th scope="col">Categoria</th>
        <th scope="col"></th>
      </tr>
    </thead>
    <tbody>
      {% for document in documents %}
        <tr>
          <th scope="row">{{document.id}}</th>
            <td>{{document.title}}</td>
            <td>
              {% for department in document.departments %}
                {{department.title}}
              {% endfor %}
            </td>
            <td>
              {% for category in document.categories %}
                {{category.title}}
              {% endfor %}
            </td>
            <td>
              <a href="/app/documents/{{document.id}}" class="btn btn-link">Atualizar</a>
              <a href="/app/documents/{{document.id}}/delete" class="btn btn-link">Remover</a>
            </td>
        </tr>
      {% endfor %}
    </tbody>
  </table>

{% endblock %}
